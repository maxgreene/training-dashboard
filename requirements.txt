# Feste Versionen = reproduzierbare Laeufe + stabiler Cache-Schluessel.
# Stand: erfolgreicher Lauf 21.07.2026. Zum Aktualisieren Version aendern,
# der naechste Lauf holt dann einmal frisch und cached neu.
#
# Direkt genutzt:
#   fitparse -> fit_streams.py              (FIT-Dateien lesen, Wahoo + Garmin)
#   garth    -> fetch_garmin.py             (Garmin Health)
#            -> fetch_garmin_activities.py  (Garmin Indoor-/Rollen-Fahrten)
# garth zieht ~30 Unterpakete (logfire, opentelemetry, pydantic, rich ...)
# automatisch mit; pip loest die aus diesen zwei Pins reproduzierbar auf.
fitparse==1.2.0
garth==0.8.0
